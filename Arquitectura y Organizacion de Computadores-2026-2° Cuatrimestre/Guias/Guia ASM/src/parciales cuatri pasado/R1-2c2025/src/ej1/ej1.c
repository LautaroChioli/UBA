#include "../ejs.h"
#include <string.h>

bool encontrarTesoroEnMapa(Mapa *mapa, Recorrido *rec, uint64_t *acciones_ejecutadas) {
    if (*mapa.habitaciones[*mapa.id_entrada].contenido.es_tesoro = true){
            return true;
        } 
    
    
    

    for (int paso = *rec.acciones[0]; paso < ACC_CANT; paso++){

        Habitacion habitacion_actual = *mapa.habitaciones[]

        if (*mapa.habitaciones[*mapa.id_entrada].contenido.es_tesoro = true){
            return true;
        } 

    }
    return 0;
}